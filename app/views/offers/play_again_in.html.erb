    <% hrs = 24 - @time_diff.ceil %>
    <% mins = (((24.hours - @time_diff)*60)%60).to_i %>
    <% sec = (((24.hours - @time_diff)*60*60)%60).to_i %>

<%= "#{hrs} hrs, #{(mins >= 0 and mins < 10) ? "0#{mins}" : mins} mins, #{sec < 10 ? "0#{sec}" : sec} sec" %>
